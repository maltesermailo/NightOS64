//
// Created by Jannik on 11.06.2024.
//

#ifndef NIGHTOS_STDINT_H
#define NIGHTOS_STDINT_H

#include_next <stdint.h>
#define MAX(x, y) (((x) > (y)) ? (x) : (y))
#define MIN(x, y) (((x) < (y)) ? (x) : (y))

#endif //NIGHTOS_STDINT_H
